/**
 * Right Panel — global widget bar (Tasks, Calendar, Messages).
 *
 * Per design system §4.4 and Accounting Hub spec AMD-03:
 *  - Visible across all modules
 *  - Content scoped to the currently selected company
 *  - Hidden entirely for External Users (Sprint 3+ permission gating)
 *
 * Sprint 0: stub — renders nothing. Sprint 9 wires up widgets.
 */
export function RightPanel() {
  return null;
}
